name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  release:
    runs-on: windows-latest

    steps:
      - name: Check out Git repository
        uses: actions/checkout@v4

      - name: Setup Windows environment
        run: |
          git config --global core.longpaths true
          npm config set script-shell cmd

      - name: Install Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: Install dependencies
        run: npm ci
        env:
          npm_config_cache: ${{ runner.temp }}/.npm
          npm_config_prefer_offline: true

      - name: Build application
        run: npm run build
        env:
          CI: true

      - name: Compress installer
        run: |
          $setupFile = Get-ChildItem -Path "release" -Filter "*Setup.exe" -Recurse | Select-Object -First 1
          $zipName = $setupFile.Name -replace '\.exe$', '.zip'
          Compress-Archive -Path $setupFile.FullName -DestinationPath "release/$zipName" -CompressionLevel Optimal
        shell: powershell

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: release-windows
          path: release/**/*.zip
          if-no-files-found: error

  create-release:
    needs: release
    runs-on: ubuntu-latest
    
    steps:
      - name: Check out Git repository
        uses: actions/checkout@v4

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          files: |
            artifacts/**/*
          generate_release_notes: true
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
